fernetKey : "your-fernet-key"  
webserverSecretKey : "your-webserver-secret-key"    
executor: "KubernetesExecutor"


postgresql:
  enabled: false

data:
  metadataConnection:
    user: airflow
    pass: airflow
    protocol: postgresql
    host: host.docker.internal
    port: 5432
    db: airflow

dags:
   gitSync:
      enabled: true
      repo: https://github.com/Ayaindeed/k8s_end2end.git
      branch : main
      rev : HEAD
      depth : 1 
      maxFailures: 0 
      subPath: "dags"